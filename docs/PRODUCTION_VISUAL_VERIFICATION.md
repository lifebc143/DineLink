# DineLink 正式站視覺與互動驗證

驗證日期：2026-08-13（GMT+8）

| 檢查項目 | 預覽／正式驗證方式 | 結果 |
| --- | --- | --- |
| 手機版版型 | 本機預覽以 390 × 844 視窗擷取首頁 | 通過：`max-w-md` 手機殼、圓角、Mesh Gradient 與飯局卡片均正確呈現。 |
| CSS 資產 | 正式網域首頁的 `dine-assets/_next/static/css/...` 以瀏覽器請求 | 通過：HTTP 200。 |
| JavaScript 資產 | 正式網域首頁的 `dine-assets/_next/static/chunks/...` 以瀏覽器請求 | 通過：HTTP 200。 |
| 視覺關鍵元素 | 正式網域 DOM 檢查 `.phone-shell` 與內嵌 `phone-shell` CSS | 通過：Mesh Gradient、字體、卡片圓角／陰影與底部導覽樣式已載入。 |
| 分頁互動 | 正式站底部導覽切換「訊息」與「探索」 | 通過：畫面切換至聊天室後可返回探索頁。 |
| 列表／地圖互動 | 正式站探索頁由「列表」切換「地圖」 | 通過：地圖提示檢視已顯示，代表前端 hydration 及狀態更新正常。 |
| API 健康檢查 | 正式站 `/` 與 `/api/events` 瀏覽器請求 | 通過：均回應 HTTP 200。 |

> 正式站以 `/dine-assets` 作為 Next.js 資產前綴，避開平台對 `/_next/static` 路徑的攔截；此策略應在後續部署中保留。

## 正式站／預覽站 DOM 回歸比對

以下快照以相同瀏覽器視窗取得，正式站與預覽站在各指定 selector 的 `className`、`width`、`maxWidth`、背景色、圓角、陰影、字體與字重完全相同。

| Selector | 驗證重點 | 比對結果 |
| --- | --- | --- |
| `.phone-shell` | `max-w-md`、`34px` 圓角、深色陰影與淡紫底色 | 相同 |
| `h1` | 31px 黑體、白字與標題間距 | 相同 |
| `button[aria-label*="詳情"]` | 飯局卡片觸發器的寬度與文字容器類別 | 相同 |
| `nav` | 底部導覽的半透明白底、毛玻璃與固定定位類別 | 相同 |

正式站另以實際操作確認：「訊息」與「探索」底部導覽可互相切換；探索頁「列表／地圖」切換能顯示地圖提示檢視。此結果與本機 390 × 844 手機預覽截圖一致。
