"use client";

import { ArrowLeft, CalendarDays, ClipboardList, ShieldCheck, Users } from "lucide-react";
import React, { useEffect, useState } from "react";

type Overview = {
  metrics: { registeredMembers: number; verifiedMembers: number; totalEvents: number; publishedEvents: number; totalApplications: number; pendingApplications: number; totalAttendances: number; noShowCount: number };
  recentMembers: Array<{ id: string; displayName: string; role: "member" | "moderator" | "admin"; verificationStatus: "unverified" | "pending" | "verified" | "rejected"; createdAt: string }>;
};

function Metric({ label, value, tone = "violet" }: { label: string; value: number; tone?: "violet" | "emerald" | "amber" | "rose" }) {
  const tones = { violet: "bg-violet-50 text-violet-900", emerald: "bg-emerald-50 text-emerald-900", amber: "bg-amber-50 text-amber-900", rose: "bg-rose-50 text-rose-900" };
  return <div className={`rounded-2xl p-3.5 ${tones[tone]}`}><p className="text-2xl font-black tabular-nums">{value.toLocaleString("zh-TW")}</p><p className="mt-1 text-[11px] font-bold opacity-70">{label}</p></div>;
}

export default function AdminDashboard({ onBack }: { onBack: () => void }) {
  const [overview, setOverview] = useState<Overview | null>(null);
  const [state, setState] = useState<"loading" | "forbidden" | "error" | "ready">("loading");
  useEffect(() => { let active = true; void fetch("/api/admin/overview", { cache: "no-store" }).then(async (response) => { if (!active) return; if (response.status === 401 || response.status === 403) { setState("forbidden"); return; } if (!response.ok) { setState("error"); return; } const payload = await response.json() as Partial<Overview>; if (!payload.metrics || !Array.isArray(payload.recentMembers)) { setState("error"); return; } setOverview(payload as Overview); setState("ready"); }).catch(() => { if (active) setState("error"); }); return () => { active = false; }; }, []);
  return <section className="page-enter mx-auto max-w-5xl px-4 pb-10 pt-5 sm:px-6"><button onClick={onBack} className="pressable mb-5 flex items-center gap-1.5 text-sm font-bold text-slate-600"><ArrowLeft className="h-4 w-4" />返回個人主頁</button><header className="rounded-[30px] bg-slate-950 p-5 text-white shadow-[0_18px_45px_rgba(27,12,62,0.25)]"><div className="flex items-start justify-between gap-4"><div><p className="text-[11px] font-bold tracking-[0.2em] text-violet-200">ADMIN CONSOLE</p><h1 className="mt-2 text-3xl font-black tracking-[-0.04em]">營運後台</h1><p className="mt-2 max-w-xl text-sm leading-relaxed text-violet-100">僅管理員可存取的會員與飯局營運摘要。數據均來自目前 PostgreSQL 資料庫。</p></div><span className="grid h-11 w-11 place-items-center rounded-2xl bg-violet-500/25 text-violet-100"><ShieldCheck className="h-5 w-5" /></span></div></header>{state === "loading" && <div className="mt-4 rounded-2xl bg-white p-5 text-sm font-semibold text-slate-500 shadow-sm">正在整理營運資料…</div>}{state === "forbidden" && <div className="mt-4 rounded-2xl border border-rose-100 bg-rose-50 p-5 text-sm leading-relaxed text-rose-800"><p className="font-black">此帳號沒有管理後台權限</p><p className="mt-1">請由既有管理員將你的角色設定為 admin 後再試。</p></div>}{state === "error" && <div className="mt-4 rounded-2xl border border-amber-100 bg-amber-50 p-5 text-sm text-amber-800">目前無法載入營運摘要，請稍後重新整理。</div>}{state === "ready" && overview && <><div className="mt-4 grid grid-cols-2 gap-3 sm:grid-cols-4"><Metric label="已註冊會員" value={overview.metrics.registeredMembers} tone="violet" /><Metric label="已驗證會員" value={overview.metrics.verifiedMembers} tone="emerald" /><Metric label="飯局總數" value={overview.metrics.totalEvents} tone="amber" /><Metric label="已發布飯局" value={overview.metrics.publishedEvents} tone="emerald" /></div><div className="mt-3 grid grid-cols-2 gap-3 sm:grid-cols-4"><Metric label="申請總數" value={overview.metrics.totalApplications} /><Metric label="待審核申請" value={overview.metrics.pendingApplications} tone="amber" /><Metric label="出席紀錄" value={overview.metrics.totalAttendances} tone="emerald" /><Metric label="爽約紀錄" value={overview.metrics.noShowCount} tone="rose" /></div><div className="mt-4 grid gap-3 sm:grid-cols-2"><div className="rounded-[24px] bg-white p-4 shadow-sm"><div className="flex items-center gap-2"><Users className="h-4 w-4 text-violet-600" /><h2 className="text-sm font-black text-slate-900">最新註冊會員</h2></div><div className="mt-3 space-y-2">{overview.recentMembers.length === 0 ? <p className="rounded-xl bg-slate-50 p-3 text-xs text-slate-500">目前尚無會員資料。</p> : overview.recentMembers.map((member) => <div key={member.id} className="flex items-center justify-between rounded-xl bg-slate-50 px-3 py-2.5"><div><p className="text-sm font-bold text-slate-800">{member.displayName}</p><p className="mt-0.5 text-[10px] text-slate-500">{new Date(member.createdAt).toLocaleDateString("zh-TW")}</p></div><span className="rounded-full bg-white px-2 py-1 text-[10px] font-bold text-slate-600">{member.verificationStatus === "verified" ? "已驗證" : member.verificationStatus === "pending" ? "驗證中" : "未驗證"}</span></div>)}</div></div><div className="rounded-[24px] bg-white p-4 shadow-sm"><div className="flex items-center gap-2"><ClipboardList className="h-4 w-4 text-emerald-600" /><h2 className="text-sm font-black text-slate-900">營運提示</h2></div><div className="mt-3 space-y-2 text-xs leading-relaxed text-slate-600"><p className="rounded-xl bg-amber-50 p-3"><CalendarDays className="mr-1 inline h-3.5 w-3.5 text-amber-700" />目前有 <b>{overview.metrics.pendingApplications}</b> 筆待主辦人審核的申請。</p><p className="rounded-xl bg-rose-50 p-3">已記錄 <b>{overview.metrics.noShowCount}</b> 筆爽約出席狀態，可持續透過信用與出席紀錄追蹤。</p><p className="rounded-xl bg-violet-50 p-3">後台僅提供必要營運摘要與最近會員顯示名稱，不顯示會員 email 或私人聊天內容。</p></div></div></div></>}</section>;
}
