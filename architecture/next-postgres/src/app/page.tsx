export default function TargetArchitecturePage() {
  return (
    <main style={{ fontFamily: "system-ui, sans-serif", margin: "0 auto", maxWidth: 640, padding: 24 }}>
      <p style={{ color: "#7c3aed", fontWeight: 700, letterSpacing: "0.12em", fontSize: 12 }}>DINELINK · NEXT.JS TARGET</p>
      <h1>Next.js App Router + PostgreSQL 遷移基線</h1>
      <p>此子專案保存正式產品的 App Router、Drizzle Schema 與領域規則。完整手機 UI 原型保留於本儲存庫的受管預覽。</p>
    </main>
  );
}
